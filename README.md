# Learn Anything

PNPM monorepo managed with Turborepo.

## Workspace layout

- `apps/` - application workspaces
- `packages/` - shared package workspaces

## Commands

```sh
pnpm dev
pnpm build
pnpm lint
pnpm test
```
